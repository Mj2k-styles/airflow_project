[project]
name = "airflow-project"
version = "0.1.0"
description = "Pipeline ETL avec Apache Airflow et OpenWeatherMap API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apache-airflow>=3.1.7",
    "pandas>=3.0.1",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
